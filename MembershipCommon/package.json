{
  "name": "MembershipCommon",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Common models for Makerspace Membership System",
  "scripts": {
    "clean": "rimraf dist",
    "build:app": "vite build",
    "build": "npm run clean && npm run build:app",
    "build:dev": "npm run clean && npm run build:app -- --mode development",
    "build:prod": "npm run clean && npm run build:app -- --mode production",
    "push": "npm run build:dev && clasp push",
    "test": "vitest"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "rollup-plugin-copy": "^3.5.0",
    "vite": "^5.4.21",
    "vitest": "^1.6.0"
  }
}
