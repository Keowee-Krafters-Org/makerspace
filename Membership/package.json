{
  "name": "Membership",
  "private": true,
  "type": "module",
    "version": "1.2.0",
    "description": "Membership Management System for Makerspace",
  "scripts": {
    "clean": "rimraf dist",
    "build:app": "vite build",
    "build:tests": "vite build -c vite.config.tests.js",
    "build": "npm run clean && npm run build:app && npm run build:tests",
    "pull": "clasp pull",
    "push": "npm run build && clasp push",
    "deploy:dev": "npm run push && node ./scripts/redeploy.mjs dev",
    "deploy:prod": "npm run push && node ./scripts/redeploy.mjs prod"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "rimraf": "^5.0.5",
    "rollup-plugin-copy": "^3.5.0",
    "vite": "^5.4.21",
    "vite-plugin-clean": "^2.0.1"
  }
}
