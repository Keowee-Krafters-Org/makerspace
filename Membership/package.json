{
  "name": "Membership",
  "private": true,
  "type": "module",
    "version": "1.3.0",
    "description": "Membership Management System for Makerspace",
  "scripts": {
    "clean": "rimraf dist",
    "build:app": "vite build",
    "build:tests": "vite build -c vite.config.tests.js",
    "build:dev": "npm run clean && npm run build:app -- --mode development && npm run build:tests -- --mode development",
    "build:prod": "npm run clean && npm run build:app -- --mode production && npm run build:tests -- --mode production",
    "build": "npm run clean && npm run build:app -- --mode development && npm run build:tests -- --mode development",
    "pull": "clasp pull",
    "push": "npm run build && clasp push",
    "deploy:dev": "node ../deploy.mjs dev",
    "deploy:prod": "node ../deploy.mjs prod"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "rimraf": "^5.0.5",
    "rollup-plugin-copy": "^3.5.0",
    "vite": "^5.4.21",
    "vite-plugin-clean": "^2.0.1"
  }
}
